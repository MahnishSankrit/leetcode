            }
                temp =temp->left;
                st.push(temp);
            while(temp != nullptr){

        while( temp != nullptr ||!st.empty()){
        
        TreeNode *temp = root;

        // st.push(root);
        stack<TreeNode *> st;

        if(root == nullptr) return arr;

        vector<int> arr;
    vector<int> inorderTraversal(TreeNode* root) {
  
