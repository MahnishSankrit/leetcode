    }
        return val;
        
        if(empty()) return -1;
    int pop() {
    
    }
        while(!st1.empty()){
            st2.push(st1.top());
        }
            st1.pop();
        int val = st2.top();
        st2.pop();
        while(!st2.empty()){
            st1.push(st2.top());
        }
            st2.pop();
