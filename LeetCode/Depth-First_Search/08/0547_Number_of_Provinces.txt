
            count++;
            visited[i] = 1;

            q.push(i);
            if(!visited[i]){
        for(int i=0; i< n; i++){
        queue<int> q;
        int count = 0;
        
        
        vector<int> visited(n, 0);
        int n=isConnected.size();
    int findCircleNum(vector<vector<int>>& isConnected) {
public:
class Solution {
