class Solution {
public:
    string largestOddNumber(string num) {
        // if(stoi(num) % 2 != 0) return num;
        int n=num.length();
        int maxVal = INT_MIN;
        string res = "";
        int  idx = -1;
        for(int i=n-1; i>=0; i--){
            if((num[i] - '0') % 2 != 0){
               idx = i;
               break;
            }
        }
        if(idx == -1) return res;
 
        for(int i=0; i<=idx; i++){
            res += num[i];
        }

