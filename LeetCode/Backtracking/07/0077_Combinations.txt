        // arr.push_back(i);
        comb(ans, n, k, i+1, arr);
        arr.pop_back();
        comb(ans, n, k, i+1, arr);
        arr.push_back(i+1);
        }
            return;
            ans.push_back(arr);
        if(arr.size() == k){
        
            }
                return;
      
        
    }
    vector<vector<int>> combine(int n, int k) {
        vector<vector<int>> ans;
