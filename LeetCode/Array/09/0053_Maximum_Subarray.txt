        for(int i=0; i<n; i++){
            sum += nums[i];
        int maxSum = INT_MIN;
            
            if(sum > maxSum){
                maxSum = max(maxSum, sum);
        int sum = 0;
      
        int maxsum = INT_MIN;
        int n = nums.size();
    int maxSubArray(vector<int>& nums) {
public:
            }

        }

            if(sum < 0) sum = 0;
