
    int m = total / 2;
    int total = arr.size();

    sort(arr.begin(), arr.end());

        arr.push_back(x);
    for(int x : nums2)

        arr.push_back(x);
    for(int x : nums1)

       vector<int> arr;
    double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
public:
class Solution {
