
        while (low <= high) {
            int mid = low + (high - low) / 2;
            int missing = (arr[mid] - (mid + 1));

            if (missing >= k) {
                high = mid - 1;
            }
        int high = n - 1;
                low = mid + 1;
            } else {
        }
            return low+k;
    }
};
