
            if(nums[mid] == target){
                return mid;
            }else if(nums[mid] > target){
                high = mid- 1;
            }else{
                low = mid + 1;
            }
            int mid = low + (high - low)/2;
       while(low <= high){

        int idx = 0;
        int high = n-1;
        int low = 0;

        int n=nums.size();
    int searchInsert(vector<int>& nums, int target) {
public:
class Solution {
