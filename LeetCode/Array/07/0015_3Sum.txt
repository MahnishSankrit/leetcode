                int z = -(nums[i] + nums[j]);
                if(mp.find(z) != mp.end()){
                    // res.push_back(ans);
                }
                    sort(ans.begin(), ans.end());
                    st.insert(ans);
                vector<int> ans ={nums[i], nums[j], z} ;
                mp[nums[j]]++;
            }
            // res.push_back(ans;)
        }
         vector<vector<int>> res(st.begin(), st.end());
        return res;
    }
};
