        for(int i=0; i<n; i++){
            if(pre == 0) pre = 1;
        }
            if(suf == 0) suf = 1;

            pre *= nums[i];
            suf *= nums[n-1-i];

            maxProd = max(max(pre, suf), maxProd);

        return maxProd;
        int maxProd = nums[0];
        
    }
};
