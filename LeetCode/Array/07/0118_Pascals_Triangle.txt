                ans = ans * (i-j);
            }
                ans = ans / j;
                arr.push_back(ans);
;            for (int j = 1; j <= i; j++) {
        }
            res.push_back(arr);
            arr.push_back(1)
        vector<vector<int>> res;
        for (int i = 0; i <n; i++) {
        vector<int> arr;
            int ans = 1;
        

        int n = numRows;
    vector<vector<int>> generate(int numRows) {
public:
class Solution {
        return res;
