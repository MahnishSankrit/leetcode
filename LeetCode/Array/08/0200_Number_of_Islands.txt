        int dx[] = {-1, 0, 1, 0};
        int dy[] = {0, -1, 0, 1};
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < m; j++) {
                if (grid[i][j] == '1' && !visited[i][j]) {
                    count++;
                    q.push({i, j});
                    visited[i][j] = 1;
                }

                while (!q.empty()) {
                    auto [cx, cy] = q.front();
                    q.pop();

                    for (int i = 0; i < 4; i++) {
                        int nx = cx + dx[i];
