            if(mp.find(sum - goal) != mp.end()){
            sum += nums[i];
                count += mp[sum - goal];
            }

        for(int i=0; i<n; i++){
     
        mp[0] = 1;
        }
        return count;
    }
            mp[sum]++;
};
        unordered_map<int, int> mp;
