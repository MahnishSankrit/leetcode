            }else if(st.top() > nums2[i]){
                mp[nums2[i]] = st.top();
                st.push(nums2[i]);
                st.push(nums2[i]);
                mp[nums2[i]] = -1;
        for(int i=n2-1; i>=0; i--){
            if(st.empty()){
            }else if(st.top() < nums2[i]){
                while(!st.empty() && st.top() <= nums2[i]){
                    st.pop();
                }
                if(st.empty()){
                    mp[nums2[i]] = -1;
                    st.push(nums2[i]);
                }else{
                    mp[nums2[i]] = st.top();
                }
                st.push(nums2[i]);
