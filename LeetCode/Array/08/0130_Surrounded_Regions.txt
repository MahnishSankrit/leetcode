            }
                }
                    visited[i][j] = 1;
                    q.push({i, j});
                if(board[i][j] == 'O' && (i == 0 || i == n-1 || j == 0 || j == m-1)){
            for(int j=0; j<m; j++){
        for(int i=0; i<n; i++){

        queue<pair<int, int>> q;
        }

        int dx[] = {-1, 0, 1, 0};
        int dy[] = {0, -1, 0, 1};

        while(!q.empty()){
        vector<vector<int>> visited(n, vector<int>(m, 0));

