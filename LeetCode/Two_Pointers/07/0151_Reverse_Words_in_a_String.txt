        }
            }

        int m = line.size();
        string  res = "";
        if(word != "")  line.push_back(word);
                word = "";
                line.push_back(word);
            }else if(s[i] == ' ' && word.length() > 0){
                word += s[i];
            
            if(s[i] != ' '){
        for(int i=0; i<n; i++){
        string word = "";
        vector<string> line;
        int n=s.length();
    string reverseWords(string s) {
public:
class Solution {
