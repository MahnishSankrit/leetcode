
        inorder(root, arr);
        int n= arr.size();
  
        for(int i=0; i<n; i++){
             int need = k - arr[i];

            if(mp.find(need) != mp.end()) return true;

            mp[arr[i]]++;
        }

        return false;

    }
};
