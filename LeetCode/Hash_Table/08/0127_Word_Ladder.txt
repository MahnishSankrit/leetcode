                q.pop();
                string word = q.front();
            while(size--){

            int size = q.size();
        while(!q.empty()){
        int level = 1;


        // if(!st.count(beginWord)) return 0;
        queue<string> q;
        q.push(beginWord);
        unordered_set<string> st(wordList.begin(), wordList.end());
    int ladderLength(string beginWord, string endWord, vector<string>& wordList) {
class Solution {
public:
