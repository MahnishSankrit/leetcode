class Solution {
public:
    int maxDepth(string s) {
       int n=s.length();
       int maxCount = INT_MIN;

       for(int i=0; i<n; i++){
            if(s[i] == '('){
       }
                count++;
            }else if(s[i] == ')'){
                maxCount = max(maxCount, count);
            }
    }
                count--;

       return maxCount; 
       int count = 0;
       if(maxCount == INT_MIN) return 0;
